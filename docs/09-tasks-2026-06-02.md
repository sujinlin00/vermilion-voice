# 09 — 2026-06-02 任务记录

## 已完成

### 1. ASR 后端统一 + 预热优化
- [x] ASR 从 GPU→WASM 回退 改为 直接 WASM（消除 session 重建开销）
- [x] Init 阶段 ASR 预热（0.5s 静音触发 WASM JIT 编译）
- [x] 效果：首句 ASR 3.7s→0.9s，RTF 0.5x→8x
- [x] 文档更新：`06-poc-results.md`、`02-architecture.md`

### 2. 流水线简化
- [x] 移除 drainQueue / chunkQueue / processingQueue（fire-and-forget 竞态）
- [x] 移除 pendingSegments 队列
- [x] 回退为简单顺序处理：`await processChunk(chunk)`
- [x] 核心修复：音频累积+fbank 在 `asrBusy` 检查**之前**执行

### 3. PUNC 回退修复
- [x] 根因：`numThreads=1` 时 VAD+PUNC 争用 WASM 单线程
- [x] 修复：PUNC 在 ASR 完成后、VAD 恢复前执行（此时 VAD 暂停）
- [x] 效果：PUNC 稳定运行，~10ms/段

### 4. 统一后端策略
- [x] VAD: WASM（小模型，无 GPU 收益）
- [x] ASR: WASM（WebGPU 必然崩溃，跳过无意义回退）
- [x] PUNC: WASM（VAD 暂停期间执行，无争用）
- [x] 移除 GPU 检测、`gpuCrashed` 标志、GPU→WASM 回退代码

### 5. UI 改进
- [x] 新增"清屏"按钮，识别中禁用，停止后可用

### 6. 文档
- [x] `02-architecture.md` — 更新为实际架构 + 双 Worker 演进分析
- [x] `06-poc-results.md` — 新增 ASR 预热章节（根因+效果+Python对比）
- [x] `07-task-plan.md` — 更新完成状态 + 技术债务
- [x] `08-dev-plan.md` — 双 Worker 开发计划（5 步骤，7h 工时）

## 当前状态

```
插件核心流水线: ✅ 稳定运行
  麦克风 → VAD(WASM, 2ms/帧) → ASR(WASM, ~8x RTF) → PUNC(WASM, 10ms) → 文本

性能基线:
  VAD: 2ms/帧, ASR: 700-900ms/6s段, PUNC: 10ms, RTF: ~8x
  段间盲区: ~1s（ASR 阻塞 VAD）

已知限制:
  - 单 Worker 顺序执行，ASR 期间 VAD 暂停
  - 单线程 WASM（Blob URL Worker 限制）
  - PUNC 无跨句上下文
```

## 待讨论：双 Worker 架构

详见 `02-architecture.md` "演进方向：双 Worker 流水线" 和 `08-dev-plan.md`。

核心权衡：
- **收益**：段间盲区 ~1s→0，VAD 持续运行，真正连续识别
- **代价**：代码量 +70%（main.ts ~500 行），7h 开发工时，内存 +3MB
- **不解决**：ASR 推理速度（单线程 WASM 硬限制）
