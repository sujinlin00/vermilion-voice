{
  "name": "vermilion-voice",
  "version": "0.1.1",
  "description": "Local speech recognition in Obsidian — ONNX WASM CPU inference. First-time model download (~500MB) requires internet; offline after setup.",
  "main": "plugin/main.js",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "obsidian": "^1.7.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@mediabunny/flac-encoder": "^1.46.0"
  }
}
